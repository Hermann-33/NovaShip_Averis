# Backend API base URL (FastAPI). Vercel: set in Project Settings > Environment Variables.
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Optional: Supabase (only needed if you wire browser-side auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
